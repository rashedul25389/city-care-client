import logo from '../../assets/logo.png';

const Logo = () => {
    return (
        <div className="flex items-end">
            <img className="w-30" src={logo} alt="" />
        </div>
    );
};

export default Logo;
